# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Impeccable — ephemeral run artifacts.
# config.json, design.json and live/config.json ARE versioned: they are the
# project's design system and detector configuration. Everything below is
# per-machine or per-run, and review/ alone is ~1MB of screenshots.
.impeccable/config.local.json
.impeccable/hook.cache.json
.impeccable/questions/
.impeccable/review/
.impeccable/live/sessions/
.impeccable/mocks/

# Agent hook configs, per-machine.
.codex/
.grok/
